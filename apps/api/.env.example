DATABASE_URL=postgresql://user:password@localhost:5432/asanaClone
PORT=4002
CORS_ORIGIN=http://localhost:5173

# Secret used to sign auth session JWTs. Generate one with:
#   openssl rand -hex 32
JWT_SECRET=

# Optional: only needed if signed cookies are used elsewhere; auth sessions don't require it.
COOKIE_SECRET=
