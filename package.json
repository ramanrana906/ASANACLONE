{
    "devDependencies": {
        "concurrently": "^10.0.5",
        "eslint": "^10.9.1",
        "husky": "^9.1.7",
        "lint-staged": "^17.4.1",
        "prettier": "^3.9.6",
        "rimraf": "^6.1.3",
        "typescript": "^5.9.3"
    },
    "name": "ASANA-clone",
    "private": true,
    "scripts": {
        "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
        "lint": "pnpm -r lint",
        "format": "prettier --write .",
        "build": "pnpm -r build",
        "clean": "rimraf apps/*/dist"
    },
    "engines": {
        "node": ">=20"
    },
    "dependencies": {
        "dotenv": "^17.4.2"
    }
}